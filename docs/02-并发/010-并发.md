创建线程：Function std::thread::spawn
https://doc.rust-lang.org/std/thread/fn.spawn.html

```rust
pub fn spawn<F, T>(f: F) -> JoinHandle<T>where
    F: FnOnce() -> T + Send + 'static,
    T: Send + 'static,
```

等待线程： t.join()

线程同步：
- 共享内存
- CSP
- Actor

共享内存：
- atomics
- 线程间共享只读数据：`Arc<T>`
- 线程间共享可写数据：`Arc<Mutex<T>>`

CSP: channel
- mpsc
- oneshot

并发数据处理